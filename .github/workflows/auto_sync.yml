- name: 提交并推送数据更改
        run: |
          git config --global user.name "CopperPriceBot"
          git config --global user.email "bot@github.com"
          
          # 🔍 强制添加，哪怕脚本没写成功也不要崩掉
          git add data/copper.csv
          
          # 检查是否有内容被暂存 (Staged)
          if ! git diff --cached --quiet; then
            echo "检测到数据变化，正在提交..."
            git commit -m "📈 自动更新铜价: $(date +'%Y-%m-%d')"
            git push
          else
            echo "文件无变化，跳过提交。"
          fi
